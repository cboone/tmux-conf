# Separator Options

> `tmux -L unconfigured -f /dev/null start-server \; show-options -g \; show-options -gw | grep separator`

```tmux
window-status-separator " "
word-separators "!\"#$%&'()*+,-./:;<=>?@[\\]^`{|}~"
```
